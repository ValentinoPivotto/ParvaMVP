# Secrets: contiene el App Secret y el access token de Meta
.env

# Base de datos local (se regenera con `make seed`)
data/*.db
data/*.db-journal
data/*.db-wal

.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# El frontend no tiene dependencias; esto es por si alguna herramienta lo crea.
node_modules/

